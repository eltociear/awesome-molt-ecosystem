# `github:` removed: GitHub Sponsors is not enabled on this account
# (hasSponsorsListing = false), so the Sponsor button dead-ended.
# https://eltociear.github.io/sponsor also 404s -- that Pages repo does not exist.
custom:
  - "https://buymeacoffee.com/eltociear"
